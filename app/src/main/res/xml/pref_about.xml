<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <Preference
        android:key="@string/pref_about_version_key"
        android:title="@string/pref_about_version_title">
    </Preference>

    <Preference
        android:key="@string/pref_about_license_key"
        android:title="@string/pref_about_license_title">
    </Preference>

    <Preference
        android:key="@string/pref_about_market_key"
        android:title="@string/pref_about_market_title">
    </Preference>

</PreferenceScreen>